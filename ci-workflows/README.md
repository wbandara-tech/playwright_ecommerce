# CI/CD Workflow Files

The workflow files need to be added manually since the API token lacks the `workflow` scope.

See the `ci-workflows/` directory for the workflow file contents, then copy them to `.github/workflows/`.
